# QueryChat
This project implements a minimal NL-to-SQL system using LangGraph and a SQL agent over a relational database (SQLite/Chinook). User questions are translated into SQL, executed on the database, and returned as structured results through a clear pipeline: input → SQL generation → execution → response.
